<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>
        <%= title %>
    </h1>

    <!-- name 속성 : 폼 요소(input, textarea ....)에 각각 이름 지정, 서버가 어떤 폼 요소에 어떤 값이 입력되었는지를 구별해줄 수 있는 key 역할 -->
    <!-- form validation -->
    <h2>GET 방식으로 요청하는 폼</h2>
    <form action="/getForm" method="get">
        <input type="text" name="id" placeholder="아이디 (최소 3글자, 필수값)" required minlength="3">
        <input type="password" name="pw" placeholder="비밀번호 (최소 4글자, 필수값)" required minlength="4">

        <!-- 이메일(필수값), 학년(1~4) input 추가 -->
         <input type="email" placeholder="이메일(필수값)" name="email" required>
         <input type="number" placeholder="학년" name="grade" min="1" max="4">
        <button type="submit">GET 방식으로 폼 제출</button>
    </form>

    <h2>POST 방식으로 요청하는 폼</h2>
    <form action="/postForm" method="post">
        <input type="text" name="id" placeholder="아이디" pattern="^([a-zA-Z0-9가-힣]){4,}$" title="대소문자, 한글, 숫자, 4글자 이상">
        <input type="password" name="pw" placeholder="비밀번호"  pattern="^([a-z0-9]){8,12}$" title="소문자, 숫자, 8글자 이상 12글자 이하">
        <button type="submit">POST 방식으로 폼 제출</button>
    </form>
</body>

</html>