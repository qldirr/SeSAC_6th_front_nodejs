<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sequelize</title>
</head>

<body>
    <h1>Sequelize를 배워보자</h1>
    <table border="1" cellspacing="0">
        <thead>
            <tr>
                <th>ID</th>
                <th>선수명</th>
                <th>나이</th>
                <th>팀</th>
        </thead>
        <tbody>
            <!-- data: db에서 가지고 오는 데이터 => 새로고침해도 데이터 남아있음 -->
            <% data.forEach(player => { %>
                <tr id="tr_<%= player.player_id %>">
                    <td>
                        <%= player.player_id %>
                    </td>
                    <td>
                        <%= player.name %>
                    </td>
                    <td>
                        <%= player.age %>
                    </td>
                    <td>
                        <%= player.team_id %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>

</body>

</html>